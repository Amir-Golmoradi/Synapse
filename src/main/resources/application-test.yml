# Test profile — stub. No @SpringBootTest currently loads the context;
# this exists so integration tests have a home when they are added.
spring:
  jpa:
    show-sql: false
logging:
  level:
    dev.amir.synapse: DEBUG