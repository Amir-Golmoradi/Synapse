# Production profile — activated by SPRING_PROFILES_ACTIVE=prod
spring:
  jpa:
    show-sql: false
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
logging:
  level:
    root: WARN
    dev.amir.synapse: INFO