# Development profile — activated by SPRING_PROFILES_ACTIVE=dev
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    dev.amir.synapse: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG